# joint_bringup

Launch-only package for the ROS 2 research layer.

`full_landing_mission.launch.py` means the full task chain is connected. It does
not imply that all Phase C research algorithms are enabled.
